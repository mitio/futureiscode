<%= form.input :confirmed_participation %>
<%= form.input :visit_dates, as: :text %>
<%= form.input :disciplines, as: :check_boxes, collection: hash_to_collection_options(Options.for('disciplines')) %>
<%= form.input :available_equipment, as: :check_boxes, collection: hash_to_collection_options(Options.for('available_equipment')) %>
<%= form.input :meetup_options, as: :radio_buttons, collection: hash_to_collection_options(Options.for('meetup_options')) %>
<%= form.input :remarks, as: :text %>
